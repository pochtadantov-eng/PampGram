diff --git a/submodules/TelegramUI/BUILD b/submodules/TelegramUI/BUILD
index 5da8c15e..cb3d5df5 100644
--- a/submodules/TelegramUI/BUILD
+++ b/submodules/TelegramUI/BUILD
@@ -467,6 +467,7 @@ swift_library(
         "//submodules/TelegramUI/Components/Gifts/GiftStoreScreen",
         "//submodules/TelegramUI/Components/Gifts/GiftSetupScreen",
         "//submodules/TelegramUI/Components/Gifts/GiftCraftScreen",
+        "//submodules/TelegramUI/Components/Gifts/PhantomGiftKit",
         "//submodules/TelegramUI/Components/ContentReportScreen",
         "//submodules/TelegramUI/Components/PeerInfo/AffiliateProgramSetupScreen",
         "//submodules/TelegramUI/Components/Stars/StarsBalanceOverlayComponent",
diff --git a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
index 1820be66..dcbfa561 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
+++ b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
@@ -48,6 +48,7 @@ swift_library(
         "//submodules/TelegramUI/Components/AlertComponent",
         "//submodules/TelegramUI/Components/AlertComponent/AlertTransferHeaderComponent",
         "//submodules/TelegramUI/Components/AvatarComponent",
+        "//submodules/TelegramUI/Components/Gifts/PhantomGiftKit",
     ],
     visibility = [
         "//visibility:public",
diff --git a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
index 429cf39c..bb752cab 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
+++ b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
@@ -28,6 +28,7 @@ import GiftViewScreen
 import UndoUI
 import EdgeEffect
 import GlassBarButtonComponent
+import PhantomGiftKit
 
 final class GiftOptionsScreenComponent: Component {
     typealias EnvironmentType = ViewControllerComponentContainer.Environment
@@ -87,7 +88,8 @@ final class GiftOptionsScreenComponent: Component {
         case resale
         case stars(Int64)
         case transfer
-        
+        case phantom
+
         init(rawValue: Int64) {
             switch rawValue {
             case 0:
@@ -100,11 +102,13 @@ final class GiftOptionsScreenComponent: Component {
                 self = .transfer
             case -4:
                 self = .resale
+            case -5:
+                self = .phantom
             default:
                 self = .stars(rawValue)
             }
         }
-        
+
         public var rawValue: Int64 {
             switch self {
             case .all:
@@ -117,6 +121,8 @@ final class GiftOptionsScreenComponent: Component {
                 return -3
             case .resale:
                 return -4
+            case .phantom:
+                return -5
             case let .stars(stars):
                 return stars
             }
@@ -187,7 +193,7 @@ final class GiftOptionsScreenComponent: Component {
                         }
                         let filteredGifts: [StarGift] = sortedGifts.filter {
                             switch self.starsFilter {
-                            case .all:
+                            case .all, .phantom:
                                 return true
                             case .limited:
                                 if case let .generic(gift) = $0 {
@@ -291,15 +297,63 @@ final class GiftOptionsScreenComponent: Component {
             self.updateScrolling(interactive: true, transition: self.nextScrollTransition ?? .immediate)
         }
         
+        // "Фантом" send: cosmetic-only, never touches Stars or the real StarGift purchase
+        // APIs. Confirms with the user first, since even a cosmetic gift shouldn't fire
+        // silently, then hands off to `PhantomGiftSender`.
+        private func openPhantomGift(gift: StarGift.Gift) {
+            guard let component = self.component, let environment = self.environment else {
+                return
+            }
+            guard let controller = environment.controller() as? GiftOptionsScreen else {
+                return
+            }
+            let mainController: ViewController
+            if let parentController = controller.parentController() {
+                mainController = parentController
+            } else {
+                mainController = controller
+            }
+            let context = component.context
+            let peerId = component.peerId
+            let title = gift.title ?? "Gift"
+
+            let theme = AlertControllerTheme(presentationData: context.sharedContext.currentPresentationData.with { $0 })
+            let font = Font.regular(floor(theme.baseFontSize * 13.0 / 17.0))
+            let alertController = textAlertController(
+                theme: theme,
+                title: NSAttributedString(string: "Фантом-подарок", font: Font.semibold(theme.baseFontSize), textColor: theme.primaryColor, paragraphAlignment: .center),
+                text: NSAttributedString(string: "Отправить «\(title)» визуально? Это не настоящая передача: ничего не будет куплено, получатель увидит анимацию, только если у него тоже установлен этот мод.", font: font, textColor: theme.primaryColor, paragraphAlignment: .center),
+                actions: [
+                    TextAlertAction(type: .genericAction, title: "Отмена", action: {}),
+                    TextAlertAction(type: .defaultAction, title: "Отправить", action: { [weak mainController] in
+                        let _ = (context.engine.data.get(TelegramEngine.EngineData.Item.Peer.Peer.init(id: context.account.peerId))
+                        |> deliverOnMainQueue).start(next: { accountPeer in
+                            let senderName = accountPeer?.compactDisplayTitle ?? "Аноним"
+                            let _ = PhantomGiftSender.send(context: context, peerId: peerId, gift: gift, senderName: senderName).start()
+                        })
+                        mainController?.dismiss()
+                    }),
+                ],
+                actionLayout: .horizontal
+            )
+            controller.present(alertController, in: .window(.root))
+        }
+
         private func openGift(gift: StarGift, skipDateCheck: Bool = false) {
             guard let component = self.component, let environment = self.environment else {
                 return
             }
+
+            if case .phantom = self.starsFilter, case let .generic(genericGift) = gift {
+                self.openPhantomGift(gift: genericGift)
+                return
+            }
+
             let context = component.context
             let strings = environment.strings
             let currentTime = Int32(CFAbsoluteTimeGetCurrent() + kCFAbsoluteTimeIntervalSince1970)
             let starGift = gift
-            
+
             if let controller = environment.controller() as? GiftOptionsScreen {
                 let mainController: ViewController
                 if let parentController = controller.parentController() {
@@ -1646,7 +1700,15 @@ final class GiftOptionsScreenComponent: Component {
                         title: strings.Gift_Options_Gift_Filter_Collectibles
                     ))
                 }
-                
+
+                // "Фантом" — cosmetic-only sends from the same real gift catalog. Kept as its
+                // own clearly-labelled tab (never merged into .all) so it can never be confused
+                // with a real, paid StarGift transfer.
+                tabSelectorItems.append(TabSelectorComponent.Item(
+                    id: AnyHashable(StarsFilter.phantom.rawValue),
+                    title: "Фантом"
+                ))
+
                 let tabSelectorSize = self.tabSelector.update(
                     transition: transition,
                     component: AnyComponent(TabSelectorComponent(
diff --git a/submodules/TelegramUI/Sources/OpenUrl.swift b/submodules/TelegramUI/Sources/OpenUrl.swift
index 6c964bad..41389f65 100644
--- a/submodules/TelegramUI/Sources/OpenUrl.swift
+++ b/submodules/TelegramUI/Sources/OpenUrl.swift
@@ -13,6 +13,7 @@ import OpenInExternalAppUI
 import BrowserUI
 import OverlayStatusController
 import PresentationDataUtils
+import PhantomGiftKit
 
 public struct ParsedSecureIdUrl {
     public let peerId: EnginePeer.Id
@@ -377,7 +378,15 @@ func openExternalUrlImpl(context: AccountContext, urlContext: OpenURLContext, ur
     guard let canonicalUrl = canonicalExternalUrl(from: url) else {
         return
     }
-    
+
+    // "Фантом" gift links never leave this client and are never resolved externally:
+    // they only exist to be caught here and turned into the cosmetic reveal. A stock
+    // Telegram build doesn't have this branch, so tapping the link there does nothing.
+    if canonicalUrl.scheme?.lowercased() == PhantomGiftLink.scheme, let payload = PhantomGiftLink.payload(from: canonicalUrl) {
+        PhantomGiftReceivedScreen.present(context: context, payload: payload)
+        return
+    }
+
     if canonicalUrl.scheme == "mailto" {
         context.sharedContext.applicationBindings.openUrl(url)
         return
