diff --git a/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift b/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift
index 2dcfc95e..c0980b72 100644
--- a/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift
+++ b/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift
@@ -152,7 +152,12 @@ private func contentNodeMessagesAndClassesForItem(_ item: ChatMessageItem) -> ([
         if let updatingMedia = itemAttributes.updatingMedia, messageMedia.isEmpty, case let .update(media) = updatingMedia.media {
             messageMedia.append(media.media)
         }
-        
+
+        // PampGram Phantom Gifts are inserted directly as real, local-only messages
+        // (see PampGramPhantomGiftMessage.insertLocalGiftMessage) carrying a genuine
+        // TelegramMediaAction.starGift, so they already fall through the normal
+        // .starGift case below — no special-casing needed here.
+
         var isFile = false
         inner: for media in messageMedia {
             if let media = media as? TelegramMediaPaidContent {
diff --git a/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/BUILD b/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/BUILD
index 81bce800..3df27238 100644
--- a/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/BUILD
+++ b/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/BUILD
@@ -32,6 +32,7 @@ swift_library(
         "//submodules/TelegramUI/Components/Chat/ChatMessageItemCommon",
         "//submodules/TelegramUI/Components/TextNodeWithEntities",
         "//submodules/InvisibleInkDustNode",
+        "//submodules/TelegramUI/Components/Gifts/PhantomGiftKit",
         "//submodules/TelegramUI/Components/PeerInfo/PeerInfoCoverComponent",
         "//submodules/TelegramUI/Components/Gifts/GiftItemComponent",
     ],
diff --git a/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/Sources/ChatMessageGiftBubbleContentNode.swift b/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/Sources/ChatMessageGiftBubbleContentNode.swift
index bf1b1cde..8666e214 100644
--- a/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/Sources/ChatMessageGiftBubbleContentNode.swift
+++ b/submodules/TelegramUI/Components/Chat/ChatMessageGiftBubbleContentNode/Sources/ChatMessageGiftBubbleContentNode.swift
@@ -23,6 +23,7 @@ import ChatMessageBubbleContentNode
 import ChatMessageItemCommon
 import TextNodeWithEntities
 import InvisibleInkDustNode
+import PhantomGiftKit
 import PeerInfoCoverComponent
 import GiftItemComponent
 
@@ -558,7 +559,7 @@ public class ChatMessageGiftBubbleContentNode: ChatMessageBubbleContentNode {
                                 buttonTitle = item.presentationData.strings.Notification_PremiumPrize_View
                                 hasServiceMessage = false
                             }
-                        case let .starGift(gift, convertStars, giftText, giftEntities, _, savedToProfile, converted, upgraded, canUpgrade, upgradeStars, isRefunded, isPrepaidUpgrade, _, channelPeerId, senderPeerId, _, _, _, _, _, toPeerId, _):
+                        case let .starGift(gift, convertStars, giftText, giftEntities, _, savedToProfile, converted, upgraded, canUpgrade, upgradeStars, isRefunded, isPrepaidUpgrade, _, channelPeerId, senderPeerId, _, prepaidUpgradeHash, _, _, _, toPeerId, _):
                             var incoming = incoming
                             var convertStars = convertStars
                             if case let .generic(gift) = gift {
@@ -689,6 +690,20 @@ public class ChatMessageGiftBubbleContentNode: ChatMessageBubbleContentNode {
                                         buttonTitle = item.presentationData.strings.Notification_StarGift_View
                                     }
                                 }
+
+                                // PampGram: cosmetic-only, locally-inserted message (see
+                                // PhantomGiftKit) — never a real StarGift, so it must never look
+                                // like one. Overrides whatever the logic above picked, unconditionally.
+                                if prepaidUpgradeHash == PampGramPhantomGiftMessage.ribbonMarker {
+                                    ribbonTitle = "Фантом"
+                                    customRibbonColors = [UIColor(rgb: 0x8e44ec), UIColor(rgb: 0xb37bf5)]
+                                    if let convertStars, convertStars > 0 {
+                                        text = "Визуальный подарок за ⭐ \(convertStars) — локально, ничего не покупалось и не передавалось по-настоящему."
+                                    } else {
+                                        text = "Визуальный подарок — локально, ничего не покупалось и не передавалось по-настоящему."
+                                    }
+                                    entities = []
+                                }
                             }
                         case let .starGiftUnique(gift, isUpgrade, _, _, _, _, isRefunded, _, _, _, _, _, _, _, _, _, fromOffer, _, isCrafted):
                             if case let .unique(uniqueGift) = gift {
@@ -762,6 +777,17 @@ public class ChatMessageGiftBubbleContentNode: ChatMessageBubbleContentNode {
                                         break
                                     }
                                 }
+
+                                // PampGram: locally-minted "collectible" Phantom Gift (see
+                                // PampGramUniqueGiftGenerator) — the slug it's tagged with never
+                                // occurs on a real unique gift. Unlike .starGift, this action type
+                                // has no spare field for a marker, so the marker lives on the gift
+                                // value itself instead.
+                                if uniqueGift.slug.hasPrefix("pampgram-phantom-") {
+                                    ribbonTitle = "Фантом"
+                                    customRibbonColors = [UIColor(rgb: 0x8e44ec), UIColor(rgb: 0xb37bf5)]
+                                    text = "Визуальный подарок — локально, ничего не покупалось и не передавалось по-настоящему."
+                                }
                             } else if isRefunded, case let .generic(gift) = gift {
                                 isStarGift = true
                                 let authorName = item.message.author.flatMap { EnginePeer($0) }?.compactDisplayTitle ?? ""
diff --git a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
index 1820be66..dcbfa561 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
+++ b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
@@ -48,6 +48,7 @@ swift_library(
         "//submodules/TelegramUI/Components/AlertComponent",
         "//submodules/TelegramUI/Components/AlertComponent/AlertTransferHeaderComponent",
         "//submodules/TelegramUI/Components/AvatarComponent",
+        "//submodules/TelegramUI/Components/Gifts/PhantomGiftKit",
     ],
     visibility = [
         "//visibility:public",
diff --git a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
index 429cf39c..30d3c8d3 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
+++ b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
@@ -28,6 +28,8 @@ import GiftViewScreen
 import UndoUI
 import EdgeEffect
 import GlassBarButtonComponent
+import PhantomGiftKit
+import ConfettiEffect
 
 final class GiftOptionsScreenComponent: Component {
     typealias EnvironmentType = ViewControllerComponentContainer.Environment
@@ -87,7 +89,8 @@ final class GiftOptionsScreenComponent: Component {
         case resale
         case stars(Int64)
         case transfer
-        
+        case phantom
+
         init(rawValue: Int64) {
             switch rawValue {
             case 0:
@@ -100,11 +103,13 @@ final class GiftOptionsScreenComponent: Component {
                 self = .transfer
             case -4:
                 self = .resale
+            case -5:
+                self = .phantom
             default:
                 self = .stars(rawValue)
             }
         }
-        
+
         public var rawValue: Int64 {
             switch self {
             case .all:
@@ -117,6 +122,8 @@ final class GiftOptionsScreenComponent: Component {
                 return -3
             case .resale:
                 return -4
+            case .phantom:
+                return -5
             case let .stars(stars):
                 return stars
             }
@@ -187,7 +194,7 @@ final class GiftOptionsScreenComponent: Component {
                         }
                         let filteredGifts: [StarGift] = sortedGifts.filter {
                             switch self.starsFilter {
-                            case .all:
+                            case .all, .phantom:
                                 return true
                             case .limited:
                                 if case let .generic(gift) = $0 {
@@ -291,15 +298,110 @@ final class GiftOptionsScreenComponent: Component {
             self.updateScrolling(interactive: true, transition: self.nextScrollTransition ?? .immediate)
         }
         
+        // "Фантом" send: cosmetic-only, never touches Stars or the real StarGift purchase
+        // APIs — see PampGramPhantomGiftManager. Deducts the local fake-Stars balance,
+        // inserts a real but local-only chat message, and stores the gift so it survives
+        // an app restart. Confirms with the user first, since even a fake spend shouldn't
+        // fire silently.
+        private func openPhantomGift(gift: StarGift.Gift) {
+            guard let component = self.component, let environment = self.environment else {
+                return
+            }
+            guard let controller = environment.controller() as? GiftOptionsScreen else {
+                return
+            }
+            let mainController: ViewController
+            if let parentController = controller.parentController() {
+                mainController = parentController
+            } else {
+                mainController = controller
+            }
+            let context = component.context
+            let peerId = component.peerId
+            let title = gift.title ?? "Gift"
+            let starPrice = gift.price
+            let asCollectible = gift.availability != nil
+
+            let _ = (PampGramPhantomGiftStore.fakeStarsBalanceSignal(context: context)
+            |> deliverOnMainQueue).start(next: { [weak controller] currentBalance in
+                guard let controller else {
+                    return
+                }
+                let theme = AlertControllerTheme(presentationData: context.sharedContext.currentPresentationData.with { $0 })
+                let font = Font.regular(floor(theme.baseFontSize * 13.0 / 17.0))
+
+                guard currentBalance >= starPrice else {
+                    controller.present(textAlertController(
+                        theme: theme,
+                        title: NSAttributedString(string: "Недостаточно Фантом-Stars", font: Font.semibold(theme.baseFontSize), textColor: theme.primaryColor, paragraphAlignment: .center),
+                        text: NSAttributedString(string: "У вас ⭐ \(currentBalance) локальных Stars, а «\(title)» стоит ⭐ \(starPrice).", font: font, textColor: theme.primaryColor, paragraphAlignment: .center),
+                        actions: [TextAlertAction(type: .defaultAction, title: "OK", action: {})],
+                        actionLayout: .horizontal
+                    ), in: .window(.root))
+                    return
+                }
+
+                let alertController = textAlertController(
+                    theme: theme,
+                    title: NSAttributedString(string: "Фантом-подарок", font: Font.semibold(theme.baseFontSize), textColor: theme.primaryColor, paragraphAlignment: .center),
+                    text: NSAttributedString(string: "Отправить «\(title)» визуально за ⭐ \(starPrice) локальных Stars (баланс: ⭐ \(currentBalance))? Это не настоящая передача — ничего не покупается, серверу Telegram ничего не отправляется.", font: font, textColor: theme.primaryColor, paragraphAlignment: .center),
+                    actions: [
+                        TextAlertAction(type: .genericAction, title: "Отмена", action: {}),
+                        TextAlertAction(type: .defaultAction, title: "Отправить", action: { [weak mainController] in
+                            let _ = (PampGramPhantomGiftManager.send(context: context, peerId: peerId, baseGift: gift, starPrice: starPrice, asCollectible: asCollectible)
+                            |> deliverOnMainQueue).start(next: { [weak mainController] result in
+                                guard let mainController, let navigationController = mainController.navigationController as? NavigationController else {
+                                    return
+                                }
+                                mainController.dismiss()
+
+                                let text: String
+                                switch result {
+                                case let .success(sendResult):
+                                    text = "Фантом-подарок «\(title)» отправлен визуально — локальный баланс: ⭐ \(sendResult.remainingBalance)."
+                                case .failure:
+                                    text = "Не удалось отправить: недостаточно локальных Stars."
+                                }
+
+                                let tooltipController = UndoOverlayController(
+                                    presentationData: context.sharedContext.currentPresentationData.with { $0 },
+                                    content: .sticker(
+                                        context: context,
+                                        file: gift.file,
+                                        loop: true,
+                                        title: nil,
+                                        text: text,
+                                        undoText: nil,
+                                        customAction: nil
+                                    ),
+                                    action: { _ in return true }
+                                )
+                                (navigationController.viewControllers.last as? ViewController)?.present(tooltipController, in: .current)
+                                navigationController.view.addSubview(ConfettiView(frame: navigationController.view.bounds))
+                            })
+                        }),
+                    ],
+                    actionLayout: .horizontal
+                )
+                controller.present(alertController, in: .window(.root))
+            })
+        }
+
         private func openGift(gift: StarGift, skipDateCheck: Bool = false) {
             guard let component = self.component, let environment = self.environment else {
                 return
             }
+
+            if case .phantom = self.starsFilter, case let .generic(genericGift) = gift {
+                self.openPhantomGift(gift: genericGift)
+                return
+            }
+
             let context = component.context
             let strings = environment.strings
             let currentTime = Int32(CFAbsoluteTimeGetCurrent() + kCFAbsoluteTimeIntervalSince1970)
             let starGift = gift
-            
+
             if let controller = environment.controller() as? GiftOptionsScreen {
                 let mainController: ViewController
                 if let parentController = controller.parentController() {
@@ -1646,7 +1748,15 @@ final class GiftOptionsScreenComponent: Component {
                         title: strings.Gift_Options_Gift_Filter_Collectibles
                     ))
                 }
-                
+
+                // "Фантом" — cosmetic-only sends from the same real gift catalog. Kept as its
+                // own clearly-labelled tab (never merged into .all) so it can never be confused
+                // with a real, paid StarGift transfer.
+                tabSelectorItems.append(TabSelectorComponent.Item(
+                    id: AnyHashable(StarsFilter.phantom.rawValue),
+                    title: "Фантом"
+                ))
+
                 let tabSelectorSize = self.tabSelector.update(
                     transition: transition,
                     component: AnyComponent(TabSelectorComponent(
