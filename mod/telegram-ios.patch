diff --git a/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift b/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift
index 2dcfc95e..4d5ad691 100644
--- a/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift
+++ b/submodules/TelegramUI/Components/Chat/ChatMessageBubbleItemNode/Sources/ChatMessageBubbleItemNode.swift
@@ -152,7 +152,12 @@ private func contentNodeMessagesAndClassesForItem(_ item: ChatMessageItem) -> ([
         if let updatingMedia = itemAttributes.updatingMedia, messageMedia.isEmpty, case let .update(media) = updatingMedia.media {
             messageMedia.append(media.media)
         }
-        
+
+        // PampGram's fake market purchases are inserted directly as real, local-only
+        // messages (see PampGramPhantomGiftMessage.insertLocalUniqueGiftMessage) carrying a
+        // genuine TelegramMediaAction.starGiftUnique, so they already fall through the
+        // normal .starGiftUnique case below — no special-casing needed here.
+
         var isFile = false
         inner: for media in messageMedia {
             if let media = media as? TelegramMediaPaidContent {
diff --git a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
index 1820be66..4590ccc6 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
+++ b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/BUILD
@@ -48,6 +48,7 @@ swift_library(
         "//submodules/TelegramUI/Components/AlertComponent",
         "//submodules/TelegramUI/Components/AlertComponent/AlertTransferHeaderComponent",
         "//submodules/TelegramUI/Components/AvatarComponent",
+        "//submodules/TelegramUI/Components/PampGram/PampGramCore",
     ],
     visibility = [
         "//visibility:public",
diff --git a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
index 429cf39c..5f26fdeb 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
+++ b/submodules/TelegramUI/Components/Gifts/GiftOptionsScreen/Sources/GiftOptionsScreen.swift
@@ -28,6 +28,7 @@ import GiftViewScreen
 import UndoUI
 import EdgeEffect
 import GlassBarButtonComponent
+import PampGramCore
 
 final class GiftOptionsScreenComponent: Component {
     typealias EnvironmentType = ViewControllerComponentContainer.Environment
@@ -87,7 +88,7 @@ final class GiftOptionsScreenComponent: Component {
         case resale
         case stars(Int64)
         case transfer
-        
+
         init(rawValue: Int64) {
             switch rawValue {
             case 0:
@@ -104,7 +105,7 @@ final class GiftOptionsScreenComponent: Component {
                 self = .stars(rawValue)
             }
         }
-        
+
         public var rawValue: Int64 {
             switch self {
             case .all:
@@ -230,6 +231,14 @@ final class GiftOptionsScreenComponent: Component {
         private var isUpdating: Bool = false
         
         private var starsStateDisposable: Disposable?
+
+        // PampGram: the local fake-Stars balance shown at the top of this screen, and
+        // whether it's shown at all — both owned by the PampGram settings screen, so
+        // they're observed live rather than read once, since the settings screen can
+        // change either while this screen is on top.
+        private var pampGramFakeStarsBalance = PampGramSettings.defaultSettings.fakeStarsBalance
+        private var pampGramFakeStarsDisplayEnabled = PampGramSettings.defaultSettings.fakeStarsDisplayEnabled
+        private var pampGramSettingsDisposable: Disposable?
         private var starsState: StarsContext.State?
         
         private let optionsPromise = Promise<[StarsTopUpOption]?>(nil)
@@ -280,6 +289,7 @@ final class GiftOptionsScreenComponent: Component {
         deinit {
             self.starsStateDisposable?.dispose()
             self.auctionDisposable.dispose()
+            self.pampGramSettingsDisposable?.dispose()
         }
 
         func scrollToTop() {
@@ -295,11 +305,12 @@ final class GiftOptionsScreenComponent: Component {
             guard let component = self.component, let environment = self.environment else {
                 return
             }
+
             let context = component.context
             let strings = environment.strings
             let currentTime = Int32(CFAbsoluteTimeGetCurrent() + kCFAbsoluteTimeIntervalSince1970)
             let starGift = gift
-            
+
             if let controller = environment.controller() as? GiftOptionsScreen {
                 let mainController: ViewController
                 if let parentController = controller.parentController() {
@@ -1011,6 +1022,20 @@ final class GiftOptionsScreenComponent: Component {
             self.appliedStarsFilter = self.starsFilter
             
             if self.component == nil {
+                self.pampGramSettingsDisposable = (PampGramCore.settingsSignal(postbox: component.context.account.postbox)
+                |> map { ($0.fakeStarsBalance, $0.fakeStarsDisplayEnabled) }
+                |> distinctUntilChanged(isEqual: { $0 == $1 })
+                |> deliverOnMainQueue).start(next: { [weak self] fakeStarsBalance, fakeStarsDisplayEnabled in
+                    guard let self else {
+                        return
+                    }
+                    self.pampGramFakeStarsBalance = fakeStarsBalance
+                    self.pampGramFakeStarsDisplayEnabled = fakeStarsDisplayEnabled
+                    if !self.isUpdating {
+                        self.state?.updated()
+                    }
+                })
+
                 self.starsStateDisposable = (component.starsContext.state
                 |> deliverOnMainQueue).start(next: { [weak self] state in
                     guard let self else {
@@ -1219,7 +1244,21 @@ final class GiftOptionsScreenComponent: Component {
                 containerSize: availableSize
             )
             
-            let formattedBalance = formatStarsAmountText(self.starsState?.balance ?? StarsAmount.zero, dateTimeFormat: environment.dateTimeFormat)
+            // While the "Локальные звёзды" PampGram toggle is on, the balance shown up top
+            // is the local fake balance on every tab ("Все"/"Мои"/"Коллекционные") — not the
+            // account's real Stars balance. It's purely a display swap: buying a gift on
+            // this screen still charges real Stars through the normal flow below,
+            // completely unaffected by what this label shows — the fake balance is only
+            // ever touched by a purchase made through the real resale market (see
+            // buyStarGiftImpl / PampGramPhantomGiftManager), which this screen doesn't lead
+            // to directly.
+            let displayedBalance: StarsAmount
+            if self.pampGramFakeStarsDisplayEnabled {
+                displayedBalance = StarsAmount(value: self.pampGramFakeStarsBalance, nanos: 0)
+            } else {
+                displayedBalance = self.starsState?.balance ?? StarsAmount.zero
+            }
+            let formattedBalance = formatStarsAmountText(displayedBalance, dateTimeFormat: environment.dateTimeFormat)
             let smallLabelFont = Font.regular(11.0)
             let labelFont = Font.semibold(14.0)
             let balanceText = tonAmountAttributedString(formattedBalance, integralFont: labelFont, fractionalFont: smallLabelFont, color: theme.actionSheet.primaryTextColor, decimalSeparator: environment.dateTimeFormat.decimalSeparator)
@@ -1646,7 +1685,7 @@ final class GiftOptionsScreenComponent: Component {
                         title: strings.Gift_Options_Gift_Filter_Collectibles
                     ))
                 }
-                
+
                 let tabSelectorSize = self.tabSelector.update(
                     transition: transition,
                     component: AnyComponent(TabSelectorComponent(
diff --git a/submodules/TelegramUI/Components/Gifts/GiftStoreScreen/Sources/GiftStoreScreen.swift b/submodules/TelegramUI/Components/Gifts/GiftStoreScreen/Sources/GiftStoreScreen.swift
index 79e9e748..d7dfe6d6 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftStoreScreen/Sources/GiftStoreScreen.swift
+++ b/submodules/TelegramUI/Components/Gifts/GiftStoreScreen/Sources/GiftStoreScreen.swift
@@ -303,6 +303,10 @@ public final class GiftStoreContentComponent: Component {
                                                     buyGift: { slug, peerId, price in
                                                         return self.starGiftsContext?.buyStarGift(slug: slug, peerId: peerId, price: price) ?? .complete()
                                                     },
+                                                    // PampGram: this is the real resale-market browsing path — the one place
+                                                    // where a fake purchase is ever offered. Reaching the same real "Buy" button
+                                                    // via .confirmPurchaseImmediately (crafting) above is untouched on purpose.
+                                                    pampGramFakePurchaseAllowed: true,
                                                     updateResellStars: { _, price in
                                                         return self.starGiftsContext?.updateStarGiftResellPrice(slug: uniqueGift.slug, price: price) ?? .complete()
                                                     }
diff --git a/submodules/TelegramUI/Components/Gifts/GiftViewScreen/BUILD b/submodules/TelegramUI/Components/Gifts/GiftViewScreen/BUILD
index 3dad3ead..cf5f2029 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftViewScreen/BUILD
+++ b/submodules/TelegramUI/Components/Gifts/GiftViewScreen/BUILD
@@ -71,6 +71,8 @@ swift_library(
         "//submodules/TelegramUI/Components/AlertComponent/AlertInputFieldComponent",
         "//submodules/TelegramUI/Components/GlassControls",
         "//submodules/Components/ResizableSheetComponent",
+        "//submodules/TelegramUI/Components/PampGram/PampGramCore",
+        "//submodules/TelegramUI/Components/PampGram/PhantomGiftKit",
     ],
     visibility = [
         "//visibility:public",
diff --git a/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewBuyGift.swift b/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewBuyGift.swift
index 6f6c1f3c..741b7e39 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewBuyGift.swift
+++ b/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewBuyGift.swift
@@ -7,6 +7,8 @@ import AccountContext
 import PresentationDataUtils
 import TelegramStringFormatting
 import BalanceNeededScreen
+import PampGramCore
+import PhantomGiftKit
 
 public func buyStarGiftImpl(
     context: AccountContext,
@@ -17,48 +19,71 @@ public func buyStarGiftImpl(
     skipConfirmation: Bool = false,
     starsTopUpOptions: Signal<[StarsTopUpOption]?, NoError>,
     buyGift: ((String, EnginePeer.Id, CurrencyAmount?) -> Signal<Never, BuyStarGiftError>)?,
+    // PampGram: only the real market-browsing → gift-detail-page → buy path sets this true
+    // (see GiftStoreScreen.swift and GiftViewScreen.swift). Every other caller — crafting
+    // included — defaults to false and keeps the exact real behaviour below, untouched.
+    pampGramFakePurchaseAllowed: Bool = false,
     getController: @escaping () -> ViewController?,
     updateProgress: @escaping (Bool) -> Void,
     updateIsBalanceVisible: @escaping (Bool) -> Void,
     completion: @escaping () -> Void
 ) {
     let presentationData = context.sharedContext.currentPresentationData.with { $0 }
-                    
-    let action: (CurrencyAmount.Currency, @escaping () -> Void) -> Void = { currency, beforeCompletion in
-        guard let resellAmount = uniqueGift.resellAmounts?.first(where: { $0.currency == currency }) else {
-            guard let controller = getController() else {
+
+    // PampGram: resolved once, up front, so both the real-balance gate below and the
+    // purchase itself agree on whether this specific attempt is faked. Callers that never
+    // opt in (pampGramFakePurchaseAllowed: false, the default) skip the Postbox read
+    // entirely and get exactly the pre-existing real behaviour.
+    let pampGramSettingsSignal: Signal<PampGramSettings, NoError> = pampGramFakePurchaseAllowed
+        ? PampGramCore.settingsSignal(postbox: context.account.postbox)
+        : .single(PampGramSettings.defaultSettings)
+
+    let _ = (pampGramSettingsSignal
+    |> take(1)
+    |> deliverOnMainQueue).start(next: { pampGramSettings in
+        let pampGramFakePurchaseActive = pampGramFakePurchaseAllowed && pampGramSettings.phantomGiftsEnabled
+
+        let action: (CurrencyAmount.Currency, @escaping () -> Void) -> Void = { currency, beforeCompletion in
+            guard let resellAmount = uniqueGift.resellAmounts?.first(where: { $0.currency == currency }) else {
+                guard let controller = getController() else {
+                    return
+                }
+                let alertController = textAlertController(
+                    context: context,
+                    title: nil,
+                    text: presentationData.strings.Gift_Buy_ErrorUnknown,
+                    actions: [TextAlertAction(type: .defaultAction, title: presentationData.strings.Common_OK, action: {})],
+                    parseMarkdown: true
+                )
+                controller.present(alertController, in: .window(.root))
+
+                beforeCompletion()
                 return
             }
-            let alertController = textAlertController(
-                context: context,
-                title: nil,
-                text: presentationData.strings.Gift_Buy_ErrorUnknown,
-                actions: [TextAlertAction(type: .defaultAction, title: presentationData.strings.Common_OK, action: {})],
-                parseMarkdown: true
-            )
-            controller.present(alertController, in: .window(.root))
-            
-            beforeCompletion()
-            return
-        }
-        
-        let proceed: () -> Void = {
-            updateProgress(true)
-            
-            let buyGiftImpl: ((String, EnginePeer.Id, CurrencyAmount?) -> Signal<Never, BuyStarGiftError>)
-            if let buyGift {
-                buyGiftImpl = { slug, peerId, price in
-                    return buyGift(slug, peerId, price)
-                }
-            } else {
-                buyGiftImpl = { slug, peerId, price in
-                    return context.engine.payments.buyStarGift(slug: slug, peerId: peerId, price: price)
+
+            let proceed: () -> Void = {
+                updateProgress(true)
+
+                let buyGiftImpl: ((String, EnginePeer.Id, CurrencyAmount?) -> Signal<Never, BuyStarGiftError>)
+                if pampGramFakePurchaseActive {
+                    buyGiftImpl = { _, peerId, price in
+                        return PampGramPhantomGiftManager.buyUniqueGift(context: context, peerId: peerId, uniqueGift: uniqueGift, price: price ?? resellAmount)
+                        |> ignoreValues
+                        |> castError(BuyStarGiftError.self)
+                    }
+                } else if let buyGift {
+                    buyGiftImpl = { slug, peerId, price in
+                        return buyGift(slug, peerId, price)
+                    }
+                } else {
+                    buyGiftImpl = { slug, peerId, price in
+                        return context.engine.payments.buyStarGift(slug: slug, peerId: peerId, price: price)
+                    }
                 }
-            }
-            
-            let finalPrice = acceptedPrice ?? resellAmount
-            let _ = (buyGiftImpl(uniqueGift.slug, recipientPeerId, finalPrice)
-            |> deliverOnMainQueue).start(error: { error in
+
+                let finalPrice = acceptedPrice ?? resellAmount
+                let _ = (buyGiftImpl(uniqueGift.slug, recipientPeerId, finalPrice)
+                |> deliverOnMainQueue).start(error: { error in
                 guard let controller = getController() else {
                     return
                 }
@@ -139,8 +164,47 @@ public func buyStarGiftImpl(
                 }
             })
         }
-        
-        if resellAmount.currency == .stars, let starsContext = context.starsContext, let starsState = context.starsContext?.currentState, starsState.balance < resellAmount.amount {
+
+        if pampGramFakePurchaseActive {
+            // PampGram: the real Stars/TON balance is irrelevant here — a fake purchase
+            // never touches it, so the real "top up"/"go to Fragment" gates below would be
+            // actively wrong (they'd block a purchase that was never going to spend real
+            // money in the first place). Checked against the fake balance instead.
+            let hasEnough: Bool
+            switch resellAmount.currency {
+            case .stars:
+                hasEnough = pampGramSettings.fakeStarsBalance >= resellAmount.amount.value
+            case .ton:
+                hasEnough = pampGramSettings.fakeTonBalanceNanos >= resellAmount.amount.value
+            }
+            if hasEnough {
+                proceed()
+            } else {
+                guard let controller = getController() else {
+                    beforeCompletion()
+                    return
+                }
+                let haveText: String
+                let needText: String
+                switch resellAmount.currency {
+                case .stars:
+                    haveText = "⭐ \(pampGramSettings.fakeStarsBalance)"
+                    needText = "⭐ \(resellAmount.amount.value)"
+                case .ton:
+                    haveText = formatTonAmountText(pampGramSettings.fakeTonBalanceNanos, dateTimeFormat: presentationData.dateTimeFormat)
+                    needText = formatTonAmountText(resellAmount.amount.value, dateTimeFormat: presentationData.dateTimeFormat)
+                }
+                let alertController = textAlertController(
+                    context: context,
+                    title: "Недостаточно средств",
+                    text: "У вас \(haveText), а нужно \(needText).",
+                    actions: [TextAlertAction(type: .defaultAction, title: presentationData.strings.Common_OK, action: {})],
+                    parseMarkdown: true
+                )
+                controller.present(alertController, in: .window(.root))
+                beforeCompletion()
+            }
+        } else if resellAmount.currency == .stars, let starsContext = context.starsContext, let starsState = context.starsContext?.currentState, starsState.balance < resellAmount.amount {
             let _ = (starsTopUpOptions
              |> filter { $0 != nil }
              |> take(1)
@@ -251,4 +315,5 @@ public func buyStarGiftImpl(
             updateIsBalanceVisible(false)
         })
     }
+    })
 }
diff --git a/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewScreen.swift b/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewScreen.swift
index 1a5228a9..2ff6b546 100644
--- a/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewScreen.swift
+++ b/submodules/TelegramUI/Components/Gifts/GiftViewScreen/Sources/GiftViewScreen.swift
@@ -1851,6 +1851,7 @@ private final class GiftViewSheetContent: CombinedComponent {
                 skipConfirmation: skipConfirmation,
                 starsTopUpOptions: self.starsTopUpOptionsPromise.get(),
                 buyGift: controller.buyGift,
+                pampGramFakePurchaseAllowed: controller.pampGramFakePurchaseAllowed,
                 getController: self.getController,
                 updateProgress: { [weak self] progress in
                     guard let self else {
@@ -5745,6 +5746,9 @@ public class GiftViewScreen: ViewControllerComponentContainer {
     fileprivate let transferGift: ((Bool, StarGiftReference, EnginePeer.Id) -> Signal<Never, TransferStarGiftError>)?
     fileprivate let upgradeGift: ((Int64?, StarGiftReference, Bool) -> Signal<ProfileGiftsContext.State.StarGift, UpgradeStarGiftError>)?
     fileprivate let buyGift: ((String, EnginePeer.Id, CurrencyAmount?) -> Signal<Never, BuyStarGiftError>)?
+    // PampGram: true only when this screen was reached by browsing the real resale market
+    // (see GiftStoreScreen.swift) — see buyStarGiftImpl's own doc for what this gates.
+    fileprivate let pampGramFakePurchaseAllowed: Bool
     fileprivate let updateResellStars: ((StarGiftReference, CurrencyAmount?) -> Signal<Never, UpdateStarGiftPriceError>)?
     fileprivate let togglePinnedToTop: ((StarGiftReference, Bool) -> Bool)?
     fileprivate let shareStory: ((StarGift.UniqueGift) -> Void)?
@@ -5766,6 +5770,7 @@ public class GiftViewScreen: ViewControllerComponentContainer {
         transferGift: ((Bool, StarGiftReference, EnginePeer.Id) -> Signal<Never, TransferStarGiftError>)? = nil,
         upgradeGift: ((Int64?, StarGiftReference, Bool) -> Signal<ProfileGiftsContext.State.StarGift, UpgradeStarGiftError>)? = nil,
         buyGift: ((String, EnginePeer.Id, CurrencyAmount?) -> Signal<Never, BuyStarGiftError>)? = nil,
+        pampGramFakePurchaseAllowed: Bool = false,
         updateResellStars: ((StarGiftReference, CurrencyAmount?) -> Signal<Never, UpdateStarGiftPriceError>)? = nil,
         togglePinnedToTop: ((StarGiftReference, Bool) -> Bool)? = nil,
         shareStory: ((StarGift.UniqueGift) -> Void)? = nil,
@@ -5782,6 +5787,7 @@ public class GiftViewScreen: ViewControllerComponentContainer {
         self.transferGift = transferGift
         self.upgradeGift = upgradeGift
         self.buyGift = buyGift
+        self.pampGramFakePurchaseAllowed = pampGramFakePurchaseAllowed
         self.updateResellStars = updateResellStars
         self.togglePinnedToTop = togglePinnedToTop
         self.shareStory = shareStory
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/BUILD b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/BUILD
index 09315ffb..a3aed400 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/BUILD
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/BUILD
@@ -140,6 +140,8 @@ swift_library(
         "//submodules/TelegramUI/Components/Settings/BoostLevelIconComponent",
         "//submodules/Components/MultilineTextComponent",
         "//submodules/TelegramUI/Components/Settings/PeerNameColorItem",
+        "//submodules/TelegramUI/Components/PampGram/PampGramSettingsUI",
+        "//submodules/TelegramUI/Components/PampGram/PampGramCore",
         "//submodules/TelegramUI/Components/PlainButtonComponent",
         "//submodules/TelegramUI/Components/TextLoadingEffect",
         "//submodules/TelegramUI/Components/TextProcessingScreen",
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreen.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreen.swift
index fd01930f..a0496a3c 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreen.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreen.swift
@@ -113,6 +113,7 @@ import PeerMessagesMediaPlaylist
 import EdgeEffect
 import Pasteboard
 import AccountPeerContextItem
+import PampGramCore
 
 public enum PeerInfoAvatarEditingMode {
     case generic
@@ -188,6 +189,7 @@ enum PeerInfoSettingsSection {
     case premiumManagement
     case stars
     case ton
+    case pampGram
 }
 
 enum PeerInfoReportType {
@@ -284,7 +286,15 @@ final class PeerInfoScreenNode: ViewControllerTracingNode, PeerInfoScreenNodePro
     let sourceMessageId: MessageId?
     var canDeleteReaction = false
     var dataDisposable: Disposable?
-    
+
+    // PampGram: live fake Stars/TON balance, read by settingsItems(...) for the "Мои
+    // звёзды"/"Мои GRAM" rows on the Settings screen. Kept on the node itself (like
+    // dataDisposable above) rather than in PeerInfoScreenData, since threading two extra
+    // fields through that struct's many construction sites elsewhere in this file would be
+    // a far larger and riskier change for what is just a display override.
+    var pampGramSettings = PampGramSettings.defaultSettings
+    var pampGramSettingsDisposable: Disposable?
+
     let activeActionDisposable = MetaDisposable()
     let resolveUrlDisposable = MetaDisposable()
     let toggleShouldChannelMessagesSignaturesDisposable = MetaDisposable()
@@ -2654,9 +2664,22 @@ final class PeerInfoScreenNode: ViewControllerTracingNode, PeerInfoScreenNodePro
                 }
             })
         }
+
+        // PampGram: keeps self.pampGramSettings current for settingsItems(...) below, and
+        // forces a relayout on change so the "Мои звёзды"/"Мои GRAM" rows pick up an edited
+        // fake balance without the user having to leave and re-enter Settings.
+        self.pampGramSettingsDisposable = (PampGramCore.settingsSignal(postbox: context.account.postbox)
+        |> deliverOnMainQueue).startStrict(next: { [weak self] settings in
+            guard let self else {
+                return
+            }
+            self.pampGramSettings = settings
+            self.requestLayout(animated: false)
+        })
     }
-    
+
     deinit {
+        self.pampGramSettingsDisposable?.dispose()
         self.dataDisposable?.dispose()
         self.hiddenMediaDisposable?.dispose()
         self.activeActionDisposable.dispose()
@@ -5479,7 +5502,7 @@ final class PeerInfoScreenNode: ViewControllerTracingNode, PeerInfoScreenNodePro
             insets.left += sectionInset
             insets.right += sectionInset
             
-            let items = self.isSettings ? settingsItems(data: self.data, context: self.context, presentationData: self.presentationData, interaction: self.interaction, isExpanded: self.headerNode.isAvatarExpanded) : infoItems(data: self.data, context: self.context, presentationData: self.presentationData, interaction: self.interaction, reactionSourceMessageId: self.reactionSourceMessageId, canDeleteReaction: self.canDeleteReaction, callMessages: self.callMessages, chatLocation: self.chatLocation, isOpenedFromChat: self.isOpenedFromChat, isMyProfile: self.isMyProfile)
+            let items = self.isSettings ? settingsItems(data: self.data, context: self.context, presentationData: self.presentationData, interaction: self.interaction, isExpanded: self.headerNode.isAvatarExpanded, pampGramSettings: self.pampGramSettings) : infoItems(data: self.data, context: self.context, presentationData: self.presentationData, interaction: self.interaction, reactionSourceMessageId: self.reactionSourceMessageId, canDeleteReaction: self.canDeleteReaction, callMessages: self.callMessages, chatLocation: self.chatLocation, isOpenedFromChat: self.isOpenedFromChat, isMyProfile: self.isMyProfile)
             
             contentHeight += headerHeight
             if !((self.isSettings || self.isMyProfile) && self.state.isEditing) {
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreenSettingsActions.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreenSettingsActions.swift
index 806e652a..af17fef0 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreenSettingsActions.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoScreenSettingsActions.swift
@@ -15,6 +15,7 @@ import TelegramPresentationData
 import PresentationDataUtils
 import PasswordSetupUI
 import InstantPageCache
+import PampGramSettingsUI
 
 extension PeerInfoScreenNode {
     func openSettings(section: PeerInfoSettingsSection) {
@@ -271,6 +272,8 @@ extension PeerInfoScreenNode {
             self.interaction.editingOpenNameColorSetup()
         case .powerSaving:
             push(energySavingSettingsScreen(context: self.context))
+        case .pampGram:
+            push(pampGramSettingsController(context: self.context))
         case .businessSetup:
             guard let controller = self.controller, !controller.presentAccountFrozenInfoIfNeeded() else {
                 return
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift
index c5f58f4b..a1b223d1 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/PeerInfoSettingsItems.swift
@@ -12,6 +12,8 @@ import ItemListPeerItem
 import DeviceAccess
 import TelegramStringFormatting
 import PeerNameColorItem
+import PampGramSettingsUI
+import PampGramCore
 
 enum SettingsSection: Int, CaseIterable {
     case edit
@@ -21,13 +23,14 @@ enum SettingsSection: Int, CaseIterable {
     case proxy
     case apps
     case shortcuts
+    case pampGram
     case advanced
     case payment
     case extra
     case support
 }
 
-func settingsItems(data: PeerInfoScreenData?, context: AccountContext, presentationData: PresentationData, interaction: PeerInfoInteraction, isExpanded: Bool) -> [(AnyHashable, [PeerInfoScreenItem])] {
+func settingsItems(data: PeerInfoScreenData?, context: AccountContext, presentationData: PresentationData, interaction: PeerInfoInteraction, isExpanded: Bool, pampGramSettings: PampGramSettings) -> [(AnyHashable, [PeerInfoScreenItem])] {
     guard let data = data else {
         return []
     }
@@ -221,6 +224,12 @@ func settingsItems(data: PeerInfoScreenData?, context: AccountContext, presentat
         interaction.openSettings(.chatFolders)
     }))
     
+    // PampGram's own section, in its own block so it reads as what it is: a mod, plainly
+    // separate from Telegram's own settings rather than blended into them.
+    items[.pampGram]!.append(PeerInfoScreenDisclosureItem(id: 0, text: "PampGram", icon: pampGramSettingsIcon(), action: {
+        interaction.openSettings(.pampGram)
+    }))
+
     let notificationsWarning: Bool
     if let settings = data.globalSettings {
         notificationsWarning = shouldDisplayNotificationsPermissionWarning(status: settings.notificationAuthorizationStatus, suppressed:  settings.notificationWarningSuppressed)
@@ -256,7 +265,19 @@ func settingsItems(data: PeerInfoScreenData?, context: AccountContext, presentat
             interaction.openSettings(.premium)
         }))
     }
-    if let starsState = data.starsState {
+    // PampGram: "Локальные звёзды" and "Локальные TON/GRAM" are independent toggles — each
+    // row falls back to stock behaviour (real balance, hidden when it's 0) on its own,
+    // regardless of what the other one is set to.
+    if pampGramSettings.fakeStarsDisplayEnabled {
+        let smallLabelFont = Font.regular(floor(presentationData.listsFontSize.itemListBaseFontSize / 17.0 * 13.0))
+        let labelFont = Font.regular(presentationData.listsFontSize.itemListBaseFontSize)
+        let labelColor = presentationData.theme.list.itemSecondaryTextColor
+        let starsFormattedLabel = formatStarsAmountText(StarsAmount(value: pampGramSettings.fakeStarsBalance, nanos: 0), dateTimeFormat: presentationData.dateTimeFormat)
+        let starsBalanceText = tonAmountAttributedString(starsFormattedLabel, integralFont: labelFont, fractionalFont: smallLabelFont, color: labelColor, decimalSeparator: presentationData.dateTimeFormat.decimalSeparator)
+        items[.payment]!.append(PeerInfoScreenDisclosureItem(id: 102, label: .attributedText(starsBalanceText), text: presentationData.strings.Settings_Stars, icon: PresentationResourcesSettings.stars, action: {
+            interaction.openSettings(.stars)
+        }))
+    } else if let starsState = data.starsState {
         if !isPremiumDisabled || abs(starsState.balance.value) > 0 {
             let balanceText: NSAttributedString
             if abs(starsState.balance.value) > 0 {
@@ -273,18 +294,22 @@ func settingsItems(data: PeerInfoScreenData?, context: AccountContext, presentat
             }))
         }
     }
-    if let tonState = data.tonState {
+    if pampGramSettings.fakeTonDisplayEnabled {
+        let smallLabelFont = Font.regular(floor(presentationData.listsFontSize.itemListBaseFontSize / 17.0 * 13.0))
+        let labelFont = Font.regular(presentationData.listsFontSize.itemListBaseFontSize)
+        let labelColor = presentationData.theme.list.itemSecondaryTextColor
+        let tonFormattedLabel = formatTonAmountText(pampGramSettings.fakeTonBalanceNanos, dateTimeFormat: presentationData.dateTimeFormat)
+        let tonBalanceText = tonAmountAttributedString(tonFormattedLabel, integralFont: labelFont, fractionalFont: smallLabelFont, color: labelColor, decimalSeparator: presentationData.dateTimeFormat.decimalSeparator)
+        items[.payment]!.append(PeerInfoScreenDisclosureItem(id: 103, label: .attributedText(tonBalanceText), text: presentationData.strings.Settings_MyTon, icon: PresentationResourcesSettings.ton, action: {
+            interaction.openSettings(.ton)
+        }))
+    } else if let tonState = data.tonState {
         if abs(tonState.balance.value) > 0 {
-            let balanceText: NSAttributedString
-            if abs(tonState.balance.value) > 0 {
-                let formattedLabel = formatTonAmountText(tonState.balance.value, dateTimeFormat: presentationData.dateTimeFormat)
-                let smallLabelFont = Font.regular(floor(presentationData.listsFontSize.itemListBaseFontSize / 17.0 * 13.0))
-                let labelFont = Font.regular(presentationData.listsFontSize.itemListBaseFontSize)
-                let labelColor = presentationData.theme.list.itemSecondaryTextColor
-                balanceText = tonAmountAttributedString(formattedLabel, integralFont: labelFont, fractionalFont: smallLabelFont, color: labelColor, decimalSeparator: presentationData.dateTimeFormat.decimalSeparator)
-            } else {
-                balanceText = NSAttributedString()
-            }
+            let formattedLabel = formatTonAmountText(tonState.balance.value, dateTimeFormat: presentationData.dateTimeFormat)
+            let smallLabelFont = Font.regular(floor(presentationData.listsFontSize.itemListBaseFontSize / 17.0 * 13.0))
+            let labelFont = Font.regular(presentationData.listsFontSize.itemListBaseFontSize)
+            let labelColor = presentationData.theme.list.itemSecondaryTextColor
+            let balanceText = tonAmountAttributedString(formattedLabel, integralFont: labelFont, fractionalFont: smallLabelFont, color: labelColor, decimalSeparator: presentationData.dateTimeFormat.decimalSeparator)
             items[.payment]!.append(PeerInfoScreenDisclosureItem(id: 103, label: .attributedText(balanceText), text: presentationData.strings.Settings_MyTon, icon: PresentationResourcesSettings.ton, action: {
                 interaction.openSettings(.ton)
             }))
diff --git a/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/BUILD b/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/BUILD
index 066e81e6..42abb362 100644
--- a/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/BUILD
+++ b/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/BUILD
@@ -51,6 +51,7 @@ swift_library(
         "//submodules/TelegramUI/Components/Gifts/GiftAnimationComponent",
         "//submodules/TelegramUI/Components/Premium/PremiumCoinComponent",
         "//submodules/TelegramUI/Components/Premium/PremiumDiamondComponent",
+        "//submodules/TelegramUI/Components/PampGram/PampGramCore",
     ],
     visibility = [
         "//visibility:public",
diff --git a/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/Sources/StarsTransactionsScreen.swift b/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/Sources/StarsTransactionsScreen.swift
index 87c58b5f..75f6326f 100644
--- a/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/Sources/StarsTransactionsScreen.swift
+++ b/submodules/TelegramUI/Components/Stars/StarsTransactionsScreen/Sources/StarsTransactionsScreen.swift
@@ -25,6 +25,7 @@ import ItemListUI
 import StarsWithdrawalScreen
 import PremiumDiamondComponent
 import StatisticsUI
+import PampGramCore
 
 private let initialSubscriptionsDisplayedLimit: Int32 = 3
 
@@ -137,7 +138,14 @@ final class StarsTransactionsScreenComponent: Component {
         
         private var stateDisposable: Disposable?
         private var starsState: StarsContext.State?
-        
+
+        // PampGram: while the feature is on, the balance shown on this screen (both the
+        // small top-right figure and the big centered one below) is the local fake
+        // balance, for both the Stars and the TON/GRAM variant of this screen — everything
+        // else here (transaction history, revenue stats, withdraw) stays real and untouched.
+        private var pampGramSettingsDisposable: Disposable?
+        private var pampGramSettings = PampGramSettings.defaultSettings
+
         private var revenueStateDisposable: Disposable?
         private var revenueState: StarsRevenueStats?
         
@@ -186,6 +194,7 @@ final class StarsTransactionsScreenComponent: Component {
         
         deinit {
             self.stateDisposable?.dispose()
+            self.pampGramSettingsDisposable?.dispose()
             self.revenueStateDisposable?.dispose()
             self.subscriptionsStateDisposable?.dispose()
         }
@@ -374,12 +383,24 @@ final class StarsTransactionsScreenComponent: Component {
                         return
                     }
                     self.revenueState = state.stats
-                    
+
                     if !self.isUpdating {
                         self.state?.updated()
                     }
                 })
-                
+
+                self.pampGramSettingsDisposable = (PampGramCore.settingsSignal(postbox: component.context.account.postbox)
+                |> deliverOnMainQueue).start(next: { [weak self] settings in
+                    guard let self else {
+                        return
+                    }
+                    self.pampGramSettings = settings
+
+                    if !self.isUpdating {
+                        self.state?.updated()
+                    }
+                })
+
                 if let subscriptionsContext = component.subscriptionsContext {
                     self.subscriptionsStateDisposable = (subscriptionsContext.state
                     |> deliverOnMainQueue).start(next: { [weak self] state in
@@ -553,11 +574,23 @@ final class StarsTransactionsScreenComponent: Component {
                 containerSize: CGSize(width: 120.0, height: 100.0)
             )
             
+            // PampGram: the local fake balance in place of the real one, on both the small
+            // top-right figure below and the big centered one further down. "Локальные
+            // звёзды" and "Локальные TON/GRAM" are independent toggles, so this screen
+            // checks whichever one matches the currency it's actually showing.
+            let pampGramFakeDisplayEnabled = component.starsContext.ton ? self.pampGramSettings.fakeTonDisplayEnabled : self.pampGramSettings.fakeStarsDisplayEnabled
+            let displayedBalance: StarsAmount
+            if pampGramFakeDisplayEnabled {
+                displayedBalance = component.starsContext.ton ? StarsAmount(value: self.pampGramSettings.fakeTonBalanceNanos, nanos: 0) : StarsAmount(value: self.pampGramSettings.fakeStarsBalance, nanos: 0)
+            } else {
+                displayedBalance = self.starsState?.balance ?? StarsAmount.zero
+            }
+
             let formattedBalance: String
             if component.starsContext.ton {
-                formattedBalance = formatTonAmountText(self.starsState?.balance.value ?? 0, dateTimeFormat: environment.dateTimeFormat)
+                formattedBalance = formatTonAmountText(displayedBalance.value, dateTimeFormat: environment.dateTimeFormat)
             } else {
-                formattedBalance = formatStarsAmountText(self.starsState?.balance ?? StarsAmount.zero, dateTimeFormat: environment.dateTimeFormat)
+                formattedBalance = formatStarsAmountText(displayedBalance, dateTimeFormat: environment.dateTimeFormat)
             }
             let smallLabelFont = Font.regular(11.0)
             let labelFont = Font.semibold(14.0)
@@ -682,14 +715,17 @@ final class StarsTransactionsScreenComponent: Component {
                                 theme: environment.theme,
                                 dateTimeFormat: environment.dateTimeFormat,
                                 title: environment.strings.Ton_AvailableBalance,
-                                value: self.revenueState?.balances.availableBalance ?? CurrencyAmount(amount: .zero, currency: .ton),
+                                // PampGram: fake TON amount, but the real, live conversion rate
+                                // — the "~$" figure this produces auto-adjusts with the market
+                                // exactly like it would for a real balance.
+                                value: self.pampGramSettings.fakeTonDisplayEnabled ? CurrencyAmount(amount: displayedBalance, currency: .ton) : (self.revenueState?.balances.availableBalance ?? CurrencyAmount(amount: .zero, currency: .ton)),
                                 rate: self.revenueState?.usdRate ?? 0.0
                             ))),
                             AnyComponentWithIdentity(id: 1, component: AnyComponent(StarsOverviewItemComponent(
                                 theme: environment.theme,
                                 dateTimeFormat: environment.dateTimeFormat,
                                 title: environment.strings.Ton_LifetimeProceeds,
-                                value: self.revenueState?.balances.overallRevenue ?? CurrencyAmount(amount: .zero, currency: .ton),
+                                value: self.pampGramSettings.fakeTonDisplayEnabled ? CurrencyAmount(amount: displayedBalance, currency: .ton) : (self.revenueState?.balances.overallRevenue ?? CurrencyAmount(amount: .zero, currency: .ton)),
                                 rate: self.revenueState?.usdRate ?? 0.0
                             )))
                         ],
@@ -754,7 +790,7 @@ final class StarsTransactionsScreenComponent: Component {
                             theme: environment.theme,
                             strings: environment.strings,
                             dateTimeFormat: environment.dateTimeFormat,
-                            count: self.starsState?.balance ?? StarsAmount.zero,
+                            count: displayedBalance,
                             currency: component.starsContext.ton ? .ton : .stars,
                             rate: nil,
                             actionTitle: component.starsContext.ton ? environment.strings.Ton_WithdrawViaFragment : (withdrawAvailable ? environment.strings.Stars_Intro_BuyShort : environment.strings.Stars_Intro_Buy),
